import React from 'react';
import PropTypes from 'prop-types';
import styles from './PlayerOptionCard.module.css';

const PlayerOptionCard = () => (
  <div className={styles.PlayerOptionCard}>
    PlayerOptionCard Component
  </div>
);

PlayerOptionCard.propTypes = {};

PlayerOptionCard.defaultProps = {};

export default PlayerOptionCard;
